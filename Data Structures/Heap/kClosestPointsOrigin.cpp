#include <iostream>
#include <queue>
#include <vector>
using namespace std;

void kClosest(vector<vector<int>> &pts, int k)
{
    k
}

int main()
{
    vector<vector<int>> pts = {   {1 , 3},
                                  {-2, 2}};
    int k = 1;
    kClosest(pts, k);
    return 0;
}